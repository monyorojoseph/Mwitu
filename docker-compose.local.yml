version: '3.8'

services:
  db:
    image: postgres:15.2-alpine
    container_name: MwituDatabase
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    ports:
      - 5432:5432
    env_file:
      - .env.db

volumes:
  postgres_data:
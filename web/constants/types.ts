export interface SiteCardType {
    id: string;
    name: string;
    cover_image: string;
    // rating: number;
}

export interface SiteDetailsType {    
    name: string;
    cover_image: string;
    about: string;
    url: string;
    // email: string;

}

export interface Item {
    label: string;
    value: string;
}